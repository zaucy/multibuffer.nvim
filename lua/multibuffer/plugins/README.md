# Example Plugins

These are some really rough plugins that I've been developing for myself. These
are not part of the multibuffer api. You should only treat these as examples
and not something to rely on. Instead a proper plugin should live on its own as
its own repository.
